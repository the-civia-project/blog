---
import LogoSVG from "../../assets/logo.svg";

interface Props {
    size?: "small" | "medium" | "big";
}

const { size = "big" } = Astro.props;

const sizePx = size === "big" ? 64 : size === "medium" ? 48 : 32;
---

<LogoSVG width={sizePx} height={sizePx} />
